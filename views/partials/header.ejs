<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> | Understand Smarter, Not Harder</title>
    <meta name="description" content="<%= description %>" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="bubble-background" aria-hidden="true">
      <span class="bubble bubble-one"></span>
      <span class="bubble bubble-two"></span>
      <span class="bubble bubble-three"></span>
      <span class="bubble bubble-four"></span>
    </div>

    <header class="topbar">
      <a class="brand" href="/">Understand Smarter</a>
      <nav class="nav-links">
        <a class="<%= page === 'home' ? 'active' : '' %>" href="/">Home</a>
        <a class="<%= page === 'ai-assistant' ? 'active' : '' %>" href="/ai-assistant">AI Assistant</a>
        <a class="<%= page === 'study-tools' ? 'active' : '' %>" href="/study-tools">Study Tools</a>
        <a class="<%= page === 'about' ? 'active' : '' %>" href="/about">About</a>
        <a class="<%= page === 'resources' ? 'active' : '' %>" href="/resources">Resources</a>
        <a class="<%= page === 'contact' ? 'active' : '' %>" href="/contact">Contact</a>
      </nav>
    </header>

    <main class="page-shell">
